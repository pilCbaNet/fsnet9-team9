import { TipoTransaccionPipe } from './tipo-transaccion.pipe';

describe('TipoTransaccionPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoTransaccionPipe();
    expect(pipe).toBeTruthy();
  });
});
